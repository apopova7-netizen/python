import turtle as t

t.tracer(0)

def square(n, a):
    if n==0:
        return
    for i in range(4):
        t.fd(a)
        t.rt(90)
    t.fd(a * 0.1)
    t.rt(8)
    square(n - 1, a * 0.9)

square(7, 300)
t.update()
